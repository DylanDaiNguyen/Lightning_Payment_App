function checklogin(req){
    if (!req.session.hasOwnProperty('userData')||!req.session.userData.ID) {
        var err = new Error('You must be logged in to view this page.');
        err.status = 401;
        console.log(err);
        return false;
    } 
    else    return true;
}

exports.checklogin = checklogin;